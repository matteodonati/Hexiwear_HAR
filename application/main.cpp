/**
 * Libraries.
 */

/* Inclusion of mbed.h */
#include "mbed.h"

/* Inclusion of model.h */
#include "model.h"

/* Inclusion of all_ops_resolver.h. */
#include "libraries/tensorflow/tensorflow/lite/micro/all_ops_resolver.h"

/* Inclusion of micro_error_reporter.h. */
#include "libraries/tensorflow/tensorflow/lite/micro/micro_error_reporter.h"

/* Inclusion of micro_interpreter.h. */
#include "libraries/tensorflow/tensorflow/lite/micro/micro_interpreter.h"

/* Inclusion of schema/schema_generated.h. */
#include "libraries/tensorflow/tensorflow/lite/schema/schema_generated.h"

/* Inclusion of version.h. */
#include "libraries/tensorflow/tensorflow/lite/version.h"

/* Inclusion of kernel_util.h. */
#include "libraries/tensorflow/tensorflow/lite/kernels/kernel_util.h"

/* Use tflite namespace. */
using namespace tflite;


/**
 * Functions definition.
 */

/* Main function. */
int main() 
{	
	/* Return zero. */
    return 0;
}